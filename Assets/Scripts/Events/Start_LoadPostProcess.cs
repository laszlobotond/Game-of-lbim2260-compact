using UnityEngine;

public class Start_LoadPostProcess : MonoBehaviour
{
    GameObject camera;
    // Start is called before the first frame update
    void Start()
    {
//        camera = GameObject.Find("");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
